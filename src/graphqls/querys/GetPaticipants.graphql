query GetPaticipants($event_id: String!) {
    getPaticipants(event_id: $event_id) {
        paticipants {
            user_id
            user_name
        }
        error {
            errorMessage
        }
    }
}
